For this level it requires converting base 64 content , 
we use the command base64 which can encode or decode content .

for encode just 
base64 , pipe the output of file to base64

for decode just 
base64 -d
pipe the output of file to base64 -d
