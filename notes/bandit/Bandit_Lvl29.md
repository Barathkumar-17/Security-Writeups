For this level the password is hidden in a dev branch , ie not the main branch , this is also a remote branch so to view these branches we use

git branch -a

git switch -detach remotes/origin/dev

will switch to that dir , get the password from readme there

