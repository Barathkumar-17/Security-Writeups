For level same as before clone the repo and read the file , this time the readme file has the password censored , for this we use git version control find the commit id in which it has been censored and copy the password.

git log

shows the log

git diff commit_id

shows the difference
