This level is very easy where we need to find the password which is after the word millionth 

we use grep which is used for pattern matching in a file

the file to search is data.txt

command:

grep "millionth" data.txt 

will return the word and the passowrd which is the next word/phrase in the file
